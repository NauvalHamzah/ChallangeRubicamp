<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CRUD (Create, Read, Update, and Delete)</title>
    <style>
        * {
            box-sizing: border-box;
        }

        body {
            background-color: rgb(211, 234, 242);
            font-family: Arial, Helvetica, sans-serif;
        }

        table {
            border-collapse: collapse;
            width: 100%;
        }

        table td,
        table th {
            border: 1px solid #ddd;
            padding: 8px;
            text-align: left;
        }

        table tr:nth-child(even) {
            background-color: #f2f2f2;
        }

        .btn {
            background-color: rgb(38, 0, 128);
            color: white;
            padding: 12px 20px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            float: left;
            width: 100%;
            text-align: center;
            text-decoration: none;
            margin-bottom: 10px;
        }

        .btn:hover {
            background-color: rgb(127, 113, 235);
        }

        .btn-save {
            background-color: rgb(3, 76, 28);
            color: white;
            padding: 12px 20px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            float: left;
            width: 100%;
            text-align: center;
            text-decoration: none;
            margin-bottom: 20px;
        }

        .btn-save:hover {
            background-color: rgba(9, 158, 31, 0.683);
        }

        input[type=text],
        input[type=date],
        input[type=number],
        select,
        textarea {
            width: 100%;
            padding: 12px;
            border: 1px solid #ccc;
            border-radius: 4px;
            resize: none;
            margin-bottom: 10px;
        }

        label {
            padding: 12px 12px 12px 0;
            display: inline-block;
        }

        /* Clear floats after the columns*/
        .row::after {
            content: "";
            display: table;
            clear: both;
        }

        /* Responsive layout - when the screen is less than 600px wide, make the two columns stack on top of each other instead of next to each other */
        @media screen and (max-width: 600px) {

            input[type=submit] {
                width: 100%;
                margin-top: 0;
            }
        }
    </style>
    <script>
        function confirmDelete(name) {
            return confirm(`Apakah kamu yakin akan menghapus data '${name}'?`);
        }
    </script>
</head>

<body>